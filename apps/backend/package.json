{
	"name": "backend",
	"private": true,
	"scripts": {
		"dev": "$(go env GOPATH)/bin/air -c ./.air.toml",
		"build": "go build -o ./bin/main .",
		"test": "go test -v ./...",
		"lint": "golangci-lint run"
	}
}
