{
	"name": "backend",
	"private": true,
	"scripts": {
		"dev": "uv run flask --app flaskr run --debug",
		"test": "uv run pytest",
		"lint": "ruff check .",
		"migrate": "echo 'No migrations yet'"
	}
}
